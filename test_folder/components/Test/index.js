import React from 'react';

const Test = (props) => (
  <div>
    { /* JSX */ }
  </div>
);

Test.propTypes = {
  title: React.PropTypes.string,
  likes: React.PropTypes.number
};

export default Test;
